/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {BranchRole} from './branch-role';

describe('BranchRole', () => {
  it('should create an instance', () => {
    expect(new BranchRole()).toBeTruthy();
  });
});
