/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BasicEntityModalComponent } from './basic-entity-modal.component';

describe('Component: BasicEntityModal', () => {
  it('should create an instance', () => {
    let component = new BasicEntityModalComponent();
    expect(component).toBeTruthy();
  });
});
